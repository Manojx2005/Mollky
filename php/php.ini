; Minimal php.ini for running the Molkky app with SQLite (no XAMPP/MySQL).
extension_dir = "ext"

extension=pdo_sqlite
extension=sqlite3
extension=mbstring
extension=openssl

; Reasonable defaults for local development.
display_errors = On
error_reporting = E_ALL
date.timezone = Asia/Tokyo
